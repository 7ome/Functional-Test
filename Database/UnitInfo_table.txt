CREATE TABLE UnitInfo(
	TransID INTEGER NOT NULL PRIMARY KEY AUTO_INCREMENT,
	Serial VARCHAR(16) NOT NULL UNIQUE,
	SKEW TEXT NOT NULL,
	BACKLASH TEXT NOT NULL,
	Date TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP());
	
ALTER TABLE UnitInfo
	ADD Date TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP;


CREATE TABLE bridgeinfo(
	TransID INTEGER NOT NULL PRIMARY KEY AUTO_INCREMENT,
	BSerial VARCHAR(16) NOT NULL UNIQUE,
	Issues TEXT NOT NULL,
	Date TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP());

CREATE TABLE issues(
	errorID INTEGER NOT NULL PRIMARY KEY AUTO_INCREMENT,
	description TEXT NOT NULL);

alter table issues auto_increment=1000;